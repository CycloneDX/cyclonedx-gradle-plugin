plugins {
    id 'java-library'
    id 'maven-publish'
}

repositories {
    mavenCentral()
    mavenLocal()
}

group = 'com.example'
version = '1.0.0'

dependencies {
    implementation group: 'com.fasterxml.jackson.datatype', name: 'jackson-datatype-jsr310', version:'2.8.11'
}

publishing {
    publications {
        maven(MavenPublication) {
            from components.java

            def cyclonedxTask = tasks.named('cyclonedxDirectBom', org.cyclonedx.gradle.CyclonedxDirectTask)
            artifact(cyclonedxTask.flatMap { it.jsonOutput }) {
                classifier = 'cyclonedx'
                extension = 'json'
            }
            artifact(cyclonedxTask.flatMap { it.xmlOutput }) {
                classifier = 'cyclonedx'
                extension = 'xml'
            }
        }
    }
    repositories {
        maven {
            url = layout.buildDirectory.dir('repo')
        }
    }
}
